import React from "react";
import { SectionHeader } from "./SectionHeader";

export default {
  title: "Components/Section Header",
  component: SectionHeader,
  tags: ["autodocs"],
};

export const Default = () => <SectionHeader>Lorem ipsum</SectionHeader>;
