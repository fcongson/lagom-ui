import {
  Meta,
  Primary,
  Controls,
  Story,
  Canvas,
  Unstyled,
} from "@storybook/blocks";
import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

<Unstyled>

# Button

Button is used to initiate actions on a page or form.

<Primary />

## Usage

Buttons allow users to initiate an action or command when clicked or tapped. The button's label or text description indicates the purpose of the action to the user.

## Props

<Controls />

## Variants

### Primary

A button can be of primary importance.

<Canvas of={ButtonStories.Primary} />

### Secondary

A button can be of secondary importance.

<Canvas of={ButtonStories.Secondary} />

### Custom

A button can be a custom variant.

<Canvas of={ButtonStories.Custom} />

## Experimental

These experimental features are hidden behind a feature flag.

### With Icon

A button can include an icon.

<Canvas of={ButtonStories.WithExperimentalIcon} />

</Unstyled>
